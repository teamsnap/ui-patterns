---
notes: |
  These classes represent the `display` property:

  + `u-flex`
  + `u-block`
  + `u-inline`
  + `u-inlineBlock`
  + `u-hidden`: `display: none`

  This classes uses trickery to be visually hidden but still available to screenreaders.
  + `u-hiddenVisually`

sourceless: true
---
